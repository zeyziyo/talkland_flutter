# General Rules
- Always communicate in Korean (unless asked otherwise).

# Build & Deployment Rules
- **NEVER** run `flutter build` commands locally.
- **ALWAYS** commit and push to `main` branch to trigger the release build.
- If a user asks for a build, explain this rule and proceed with git push.

# Project Structure
- This project uses GitHub Actions for CI/CD.
